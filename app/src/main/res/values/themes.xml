<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MathDices" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MathDices" parent="Base.Theme.MathDices" >
        <item name="android:button">@drawable/rounded_move_button</item>
        <item name="android:background">@null</item>
    </style>
</resources>